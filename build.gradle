apply plugin: 'base'

group = 'com.axway.apigw'
version = '7.7.0.20260228-3'

ext {
	bndBuilderVersion = '6.4.0'
}

subprojects {
	apply plugin: 'eclipse'

	group = 'com.axway.apigw'
	version = '7.7.0.20260228-3'
}

task copyArchives(type: Copy) {
	from subprojects.collect { it.tasks.withType(AbstractArchiveTask) }
	into "$buildDir/archives"
}

clean {
	delete "${buildDir}"
}
