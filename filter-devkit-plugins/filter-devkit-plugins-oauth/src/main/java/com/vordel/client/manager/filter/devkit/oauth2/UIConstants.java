package com.vordel.client.manager.filter.devkit.oauth2;

/**
 * Specific class for Scopes Match (since the original OAuth 2 filter does not export its UI package).
 */
public interface UIConstants {
	public static final String[] SCOPES_MATCH = new String[]{"Any", "All"};
}
